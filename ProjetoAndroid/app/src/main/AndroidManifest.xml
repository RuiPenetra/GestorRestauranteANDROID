<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.GRestauranteAPP">
    <!-- Esta Linha da permissao para aceder a net -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Esta Linha da permissao para fazer chamada -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.GRestauranteAPP.ContactosActivity"></activity>
        <activity android:name="com.example.GRestauranteAPP.RegisterActivity" />
        <activity android:name="com.example.GRestauranteAPP.LoginActivity" />
        <activity android:name="com.example.GRestauranteAPP.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>